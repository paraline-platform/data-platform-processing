# ============================================================
# GITLEAKS CONFIG — data-platform-processing
# ============================================================
[extend]
useDefault = true

[allowlist]
description = "Dev-only S3A credentials trong spark-jobs — TODO(P2.2): xoá khi creds chuyển sang secretRef env"
paths = [
  # TODO(P2.2): fs.s3a.access.key/secret.key chuyển sang envFrom secretRef (minio-s3a)
  '''spark-jobs/iceberg-test\.yaml''',
  '''spark-jobs/lakehouse-namespace-setup\.yaml''',
]
